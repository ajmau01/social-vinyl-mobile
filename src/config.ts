export const CONFIG = {
    // Use 10.0.2.2 for Android Emulator to reach localhost on host machine
    // Use localhost for iOS Simulator
    // Use local IP for physical devices (Replace with your machine's IP if testing on device)
    API_URL: 'http://localhost:9080',
    WS_URL: 'ws://localhost:9080/listening-bin',
};
